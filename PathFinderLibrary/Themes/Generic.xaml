<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PathFinderLibrary">


    <!--<DataTemplate DataType="{x:Type local:Path}">

        <ContentPresenter>
            <ContentPresenter.Content>

                <Polyline x:Name="poly"
                      Stroke="{Binding Path=Fill,FallbackValue=Gray}"
                      Points="{Binding Path=WayPoints, Converter={x:Static local:ConnectionPathConverter.Instance}}"
                       StrokeThickness="2" />

            </ContentPresenter.Content>
        </ContentPresenter>
   
    </DataTemplate>-->


    <!--<Style  TargetType="{x:Type local:PathPolyLine}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PathPolyLine}">
                    <Polyline     Name="PolyLine"  
                                  Stroke="Gray"
                                  StrokeThickness="2" 
                                  Points="{Binding ConnectionPoints,Mode=OneWay,RelativeSource={RelativeSource TemplatedParent}}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

</ResourceDictionary>